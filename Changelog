- escapes: CHECKLATENCY
- fixed performance graphs
- login-screen for web-interface
- latency graph in web-interface
- can add a logo
- header can be set to always draw with backgroundcolor (instead of state-color)
- can now let the background color depend on the state
- configurable bar height between header and problem-list
- can now retrieve the nagios status via https and/or authetication as well
